doctrine:
    dbal:
        # Configuration utilisee uniquement en production.
        # Elle evite que le serveur Alwaysdata interprete mal les options MySQL
        # passees dans DATABASE_URL.
        server_version: 'mariadb-11.4.0'
        charset: 'utf8'
