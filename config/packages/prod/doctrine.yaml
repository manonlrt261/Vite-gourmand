doctrine:
    dbal:
        # Configuration utilisee uniquement en production.
        # Alwaysdata utilise MariaDB : on force une version complete et un charset
        # compatible avec son pilote MySQL pour eviter l'erreur "Unknown character set".
        server_version: 'mariadb-11.4.0'
        charset: 'utf8'
