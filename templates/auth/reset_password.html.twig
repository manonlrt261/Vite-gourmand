{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe - Vite & Gourmand{% endblock %}

{% block body %}
{% include 'components/_big_title.html.twig' with {
    title: 'Réinitialisation',
    image: asset('assets/bigtittle.png'),
    image_alt: 'Table gourmande Vite et Gourmand'
} %}

<main class="forgot-page">
    <section class="forgot-card forgot-card--reset">
        <div class="forgot-card__header">
            <h1>Nouveau mot de passe</h1>
            <p>Choisissez un nouveau mot de passe pour accéder à votre compte.</p>
            <span aria-hidden="true"></span>
        </div>

        {% for message in app.flashes('reset_error') %}
            <div class="auth-alert auth-alert--error">{{ message }}</div>
        {% endfor %}

        <form class="forgot-form" method="post">
            <label class="forgot-field">
                <span>Nouveau mot de passe</span>
                <input type="password" name="password" placeholder="Votre nouveau mot de passe" autocomplete="new-password" required>
            </label>

            <label class="forgot-field">
                <span>Confirmer le mot de passe</span>
                <input type="password" name="password_confirm" placeholder="Confirmez votre mot de passe" autocomplete="new-password" required>
            </label>

            <button type="submit" class="buttonprimary forgot-submit">Réinitialiser le mot de passe</button>
        </form>
    </section>
</main>
{% endblock %}
