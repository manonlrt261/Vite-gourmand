{% extends 'base.html.twig' %}

{# Analyse du chiffre d'affaires par menu avec filtres et graphiques côté navigateur. #}
{% block title %}Chiffre d'affaires par menu - Vite & Gourmand{% endblock %}

{% block body %}
    {% include 'components/_big_title.html.twig' with {
        title: "Chiffre d'affaires par menu",
        image: asset('assets/bigtittle.png'),
        image_alt: "Chiffre d'affaires par menu Vite et Gourmand"
    } %}

    {# L'attribut racine permet au script de s'initialiser uniquement sur cette page. #}
    <main class="admin-orders-menu-page" data-revenue-menu-page>
        <a href="{{ path('admin_dashboard') }}" class="cart-back-link">&larr; Retour &agrave; l'espace administrateur</a>

        {# Partie haute : filtres de recherche à gauche et indicateurs clés à droite. #}
        <section class="admin-orders-menu-top">
            {# Formulaire de filtres traité en JavaScript pour actualiser les résultats sans rechargement. #}
            <form class="admin-orders-menu-filters" data-revenue-menu-filters>
                <h1>Filtres</h1>

                <label>
                    <span>Date de d&eacute;but</span>
                    <input type="date" name="start_date" data-revenue-menu-start>
                </label>

                <label>
                    <span>Date de fin</span>
                    <input type="date" name="end_date" data-revenue-menu-end>
                </label>

                {# Filtre par thème de menu. #}
                <label>
                    <span>Th&egrave;me</span>
                    <select name="theme" data-revenue-menu-theme>
                        <option value="">Tous les th&egrave;mes</option>
                        {% for theme in themes %}
                            <option value="{{ theme }}">{{ theme }}</option>
                        {% endfor %}
                    </select>
                </label>

                {# Filtre multi-sélection permettant de comparer plusieurs menus précis. #}
                <div class="admin-orders-menu-field">
                    <span>Menus</span>
                    <div class="admin-orders-menu-checkboxes" data-revenue-menu-select>
                        {% for menu in menus %}
                            <label>
                                <input type="checkbox" name="menus[]" value="{{ menu.menu_id }}">
                                <span>{{ menu.nom_menu }}</span>
                            </label>
                        {% endfor %}
                    </div>
                </div>

                <div class="admin-orders-menu-filter-actions">
                    <button type="button" class="buttonprimary" data-revenue-menu-apply>Appliquer les filtres</button>
                    <button type="button" class="buttonoffwhite" data-revenue-menu-reset>R&eacute;initialiser</button>
                </div>
            </form>

            {# Indicateurs résumés affichés au chargement de la page. #}
            <div class="admin-orders-menu-stats">
                <article>
                    <strong data-revenue-menu-total>{{ initialStats.revenue|number_format(2, ',', ' ') }} &euro;</strong>
                    <span>CA total en {{ initialStats.year }}</span>
                </article>

                <article>
                    <strong>{{ initialStats.active_menus_count }}</strong>
                    <span>Menus disponibles</span>
                </article>

                <article class="admin-orders-menu-stats-wide">
                    <strong>{{ initialStats.themes_count }}</strong>
                    <span>Th&egrave;mes</span>
                </article>
            </div>
        </section>

        {# Zone des graphiques construits côté navigateur à partir des données JSON. #}
        <section class="admin-orders-menu-charts" aria-label="Graphiques du chiffre d'affaires par menu">
            {# Graphique circulaire : répartition du chiffre d'affaires par thème. #}
            <article class="admin-orders-menu-panel">
                <h2>R&eacute;partition du CA par th&egrave;me</h2>
                <div class="admin-orders-menu-donut-layout">
                    <div class="admin-orders-menu-donut" data-revenue-menu-donut>
                        <span data-revenue-menu-donut-total>0 &euro;</span>
                        <small>total</small>
                    </div>
                    <div class="admin-orders-menu-legend" data-revenue-menu-legend></div>
                </div>
            </article>

            {# Graphique en barres : comparaison du chiffre d'affaires généré par chaque menu. #}
            <article class="admin-orders-menu-panel">
                <h2>Chiffre d'affaires par menu</h2>
                <div class="admin-orders-menu-bar-chart" data-revenue-menu-bars></div>
            </article>
        </section>

        {# Tableau détaillé : il affiche les valeurs exactes derrière les graphiques. #}
        <section class="admin-orders-menu-panel admin-orders-menu-detail">
            <h2>D&eacute;tail par menu</h2>

            <div class="admin-orders-menu-table-wrap">
                <table class="admin-orders-menu-table">
                    <thead>
                        <tr>
                            <th>Menu</th>
                            <th>Th&egrave;me</th>
                            <th>Chiffre d'affaires</th>
                            <th>%</th>
                        </tr>
                    </thead>
                    <tbody data-revenue-menu-table></tbody>
                </table>
            </div>

            {# Message affiché si aucun chiffre d'affaires ne correspond aux filtres choisis. #}
            <p class="employee-empty" data-revenue-menu-empty hidden>Aucun chiffre d'affaires ne correspond aux filtres.</p>
        </section>

        {# Encadré d'information pour expliquer la source et la limite des données affichées. #}
        <section class="admin-orders-menu-info">
            <span aria-hidden="true">i</span>
            <div>
                <p>Les donn&eacute;es affich&eacute;es sont calcul&eacute;es automatiquement &agrave; partir des commandes enregistr&eacute;es.</p>
                <p>Les commandes annul&eacute;es ne sont pas comptabilis&eacute;es dans ces rapports.</p>
            </div>
        </section>
    </main>

    {# Charge utile JSON rendue côté serveur, lue puis filtrée par le script de la page. #}
    <script type="application/json" id="revenue-menu-documents">{{ documentsJson|raw }}</script>
    {# Valeurs de référence utilisées avant toute application de filtre. #}
    <script type="application/json" id="revenue-menu-default-stats">{{ initialStats|json_encode|raw }}</script>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {# Script qui filtre les données et génère les graphiques du chiffre d'affaires. #}
    <script src="{{ asset('assets/scripts/admin-revenue-by-menu.js') }}" defer></script>
{% endblock %}
